/*
// Created by Qi7 on 2023/9/21.
#include "stdio.h"
int main(void)
{
   int UpperLimit, LowerLimit, Difference;
    printf("Please enter the upper limit and the lower limit.\n");
    scanf("%d %d", &UpperLimit, &LowerLimit);
    Difference = UpperLimit - LowerLimit;
    for (int i = 0; i <= Difference; ++i)
    {
        printf("%d %d %d\n", LowerLimit, LowerLimit * LowerLimit, LowerLimit * LowerLimit * LowerLimit);
        LowerLimit++;
    }
    return 0;
}*/
